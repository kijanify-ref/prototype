name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json
      - name: Install
        run: npm ci
      - name: Build static export
        run: npm run build
        env:
          NEXT_PUBLIC_BASE_PATH: /prototype
      - name: Add development brief
        working-directory: .
        run: |
          mkdir -p web/out/brief/assets
          cp "PT자료/brief.html" web/out/brief/index.html
          cp "PT자료/brief.html" web/out/brief/brief.html
          cp "PT자료/brief.css" "PT자료/brief.js" "PT자료/brief-ui.js" "PT자료/presentation.css" web/out/brief/
          cp "PT자료/assets/"* web/out/brief/assets/
      - name: Deploy to gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: web/out
          force_orphan: true
          enable_jekyll: false
